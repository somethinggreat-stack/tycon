<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Serve the app from the public/ sub-folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Never expose the environment file
<Files ".env">
    Require all denied
</Files>